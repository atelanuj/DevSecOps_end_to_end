# Output

- **`outputs.tf`**: Here, you specify outputs that Terraform will display after applying the configuration, such as resource IDs or IP addresses, which can also be passed to other parts of your project.

```hcl
# Optional: Output the found AMI ID
output "latest_amazon_linux_2023_ami_id" {
  description = "The ID of the latest Amazon Linux 2023 AMI found."
  value       = data.aws_ami.ubuntu.id
}

output "aws_instance_public_ip" {
  description = "The public IP address of the EC2 instance."
  value       = aws_instance.test-ec2.public_ip
}
```

```bash
# output Looks like this!!
Apply complete! Resources: 1 added, 0 changed, 0 destroyed.

Outputs:

aws_instance_public_ip = "3.110.135.231"
latest_amazon_linux_2023_ami_id = "ami-00e6f0506378959e2"
```