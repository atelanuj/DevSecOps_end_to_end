version: '3'
services:
  ubuntu_custom:
    image: ubuntu_custom:v1
    build: .
    container_name: ubuntu_custom
    volumes:
      - ${PWD}:/app
    working_dir: /app
    command: [ "bash" ]
    stdin_open: true # interactive
    tty: true # tty
