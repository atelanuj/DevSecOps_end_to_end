version: '3'
services:
  dind:
    image: dind:v1
    build: .
    container_name: dind
    volumes:
      - ${PWD}:/app
    working_dir: /app
    command: [ "bash" ]
    stdin_open: true # interactive
    tty: true # tty
