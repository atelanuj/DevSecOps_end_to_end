version: '3'
services:
  ubuntu-dind:
    image: anujatel/ubuntu-dind:v1
    build: ./Dockerfile-Dind-ubuntu-mlops
    container_name: ubuntu-dind
    volumes:
      - ${PWD}:/app
    working_dir: /app
    command: [ "bash" ]
    stdin_open: true # interactive
    tty: true # tty
